import { createStore } from 'vuex'

export default createStore({
  state(){
    return{
      windowSize:{
        windowSizeWidth:1000,
        windowSizeHeight:1000
      }
    }
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
